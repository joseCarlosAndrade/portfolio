# Profile photo

Put your photo here:

- **Filename:** `photo.jpg` (or use `photo.png` — then change `images/photo.jpg` to `images/photo.png` in `index.html`)

The hero section uses a 160×160px circle; any square or portrait image will be cropped to fit. Recommended: at least 320×320px for sharp display on retina.
